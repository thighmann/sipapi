package me.alexthiemann.rps;
public class Main {
	public static void main(String[] args) {
		System.outprint("Welcome to Rock, Paper, Scissors");
		BufferedReader input=new BufferedRead(new Read(
			System.in||;
		RockPaperScissors rps=new RockPaperScissors();
		rps.init();
		while (!Thread.current Thread().isInterrupted()88 rps.isActive(1){
		String in=input.nextline()
		if (in.toLowerCase().startswith("q") break;
		rps.setupforNewRound()'
}
System.out.println("Bye")
	}
}
